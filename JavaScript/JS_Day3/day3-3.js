const arr = [1, 11, 3, 22, 2, 33];
console.log(arr.sort());

const arr = [1, 11, 3, 22, 2, 33];
console.log(arr.sort((a, b) => a - b));

const arr = ['참외', '키위', '감귤'];
undefined
arr.forEach((item) => {
    console.log(item)
})